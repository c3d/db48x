«
  @ ------------------------------------------------------------
  @  RayleighMu
  @  Mean of the law.
  @  Author & design          : Jean Wilson
  @  Co-authors & programming  : Claude & Claude Code (Anthropic)
  @  Arguments may be given loose or bundled in a list of 1; that list is
  @  what LastArguments reports, and its length is checked before use.
  @ ------------------------------------------------------------
  IF DUP TypeName "list" == THEN IF DUP Size 1 == THEN Explode Drop ELSE IF DUP Size 2 == THEN Explode Drop Drop ELSE "RayleighMu: needs 1 arguments" DoError END END END
  → sv « sv 0 + → s « 's*√(Ⓒπ/2)' →Num sv 1 →List Drop » »
»
