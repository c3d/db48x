«
  @ ------------------------------------------------------------
  @  ParetoPDF
  @  Density (or mass) at X.
  @  Author & design          : Jean Wilson
  @  Co-authors & programming  : Claude & Claude Code (Anthropic)
  @  Arguments may be given loose or bundled in a list of 3; that list is
  @  what LastArguments reports, and its length is checked before use.
  @ ------------------------------------------------------------
  IF DUP TypeName "list" == THEN IF DUP Size 3 ≠ THEN "ParetoPDF: needs 3 arguments" DoError END Explode Drop END
  → xmv apv Xv « xmv 0 + apv 0 + Xv 0 + → xm ap X « 'IFTE(X<xm;0;ap*(xm^ap)/(X^(ap+1)))' →Num xmv apv Xv 3 →List Drop » »
»
