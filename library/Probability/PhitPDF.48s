«
  @ ------------------------------------------------------------
  @  PhitPDF
  @  Density (or mass) at X.
  @  Author & design          : Jean Wilson
  @  Co-authors & programming  : Claude & Claude Code (Anthropic)
  @  Arguments may be given loose or bundled in a list of 1; that list is
  @  what LastArguments reports, and its length is checked before use.
  @ ------------------------------------------------------------
  IF DUP TypeName "list" == THEN IF DUP Size 1 ≠ THEN "PhitPDF: needs 1 arguments" DoError END Explode Drop END
  → Xv « Xv 0 + → X « 'EXP(-(X^2)/2)/√(2*Ⓒπ)' →Num Xv 1 →List Drop » »
»
