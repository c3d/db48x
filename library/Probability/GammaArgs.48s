«
  @ ------------------------------------------------------------
  @  GammaArgs
  @  Returns the parameters tagged with their names.
  @  Author & design          : Jean Wilson
  @  Co-authors & programming  : Claude & Claude Code (Anthropic)
  @  Arguments may be given loose or bundled in a list of 3; that list is
  @  what LastArguments reports, and its length is checked before use.
  @ ------------------------------------------------------------
  IF DUP TypeName "list" == THEN IF DUP Size 3 ≠ THEN "GammaArgs: needs 3 arguments" DoError END Explode Drop END
  → av scv Xv « av 0 + scv 0 + Xv 0 + → a sc X « a "a" →Tag sc "sc" →Tag X "X" →Tag av scv Xv 3 →List Drop » »
»
